# hermes-voice — голосовой слой для Hermes Agent
# Все пакеты бесплатные и работают локально, без внешних API-ключей.

fastapi>=0.115
uvicorn[standard]>=0.32

# Распознавание речи (локально, CTranslate2)
faster-whisper>=1.1

# Синтез речи (локально, ONNX). Основной движок.
piper-tts>=1.8

# Синтез речи (облако Microsoft). Опциональный движок: голосов больше,
# но первый звук от 2 с и растёт с длиной текста.
edge-tts>=7.0

httpx>=0.27
numpy>=1.26
python-multipart>=0.0.12
